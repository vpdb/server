// Typography
//
// Below are different text styles used on VPDB.
//
// Styleguide 1.2


// Headings
//
// Your standard, everyday headings. The pure headers define font, weight and absolute
// size, while the modifiers take care of color, relative size and special positioning.
// Note that `.h4` headers are usually used by panel headers. Also note that headers are
// per default oneliners, i.e. they don't line wrap but are cut off with an ellipsis.
//
//   * Color - `.h--primary` which changes the color, leaving type and size as-is
//   * Size -  `.h--small`, `.h--smalleer` which changes the size, leaving the type and color as-is
//
// .             - Default styles
// .h--primary   - Header in primary color
// .h--small     - Small font size (80%)
// .h--smaller   - Smaller font size
// .h--no-margin - Without margin, when padding is given by parent.
//
// Markup:
// <h1 class="{$modifiers}">Heading h1</h1>
// <h2 class="{$modifiers}">Heading h2</h2>
// <h3 class="{$modifiers}">Heading h3</h3>
// <h4 class="{$modifiers}">Heading h4</h4>
// <h5 class="{$modifiers}">Heading h5</h5>
// <h6 class="{$modifiers}">Heading h6</h6>
//
// Styleguide 1.2.1

// Page headers, Sidebar header, anything that is upper case and title font
h1
	font-family $font-header
	font-size $font-size-larger-alt
	font-weight $font-weight-normal-alt
	text-transform uppercase

// Release title
h2
	font-family $font-content
	font-weight $font-weight-bold
	font-size $font-size-largest
	text-transform none

// Huge modal title
h3
	font-family $font-content
	font-weight $font-weight-thin
	font-size $font-size-huge
	text-transform none
	margin-bottom 15px

// Panel headers
h4
	font-family $font-content
	font-weight $font-weight-bold
	font-size $font-size-normal
	text-transform uppercase

// Small sidebar headers
h5
	font-family $font-content
	font-weight $font-weight-normal
	font-size $font-size-normal
	text-transform uppercase
h6
	font-family $font-content
	font-weight $font-weight-bold
	font-size $font-size-normal
	text-transform none

h1.h--small
	font-size $font-size-normal-alt
h2.h--small
	font-size $font-size-larger
h1.h--smaller
	font-size $font-size-small-alt
h4.h--large
	font-size $font-size-large

h1, h2, h3, h4, h5, h6
	white-space nowrap
	overflow hidden
	text-overflow ellipsis
	line-height normal
	&.h--no-margin
		margin 0

// Fix vetical position of floating h3
h1 + h1.pull-right
	margin-top -48px
	margin-bottom 0

// Panel headings
// -------------------------
.panel__h-alpha // formerly: .panel h1
	margin-top 0
	font-size $font-size-largest
	font-family $font-content
	font-weight $font-weight-bold
	text-transform none

.panel__h-beta // formerly: .panel h3
	font-size $font-size-larger
	font-family $font-header
	font-weight $font-weight-normal-alt
	text-transform uppercase
	white-space nowrap
	overflow hidden
	text-overflow ellipsis

.panel__h-gamma // formerly: .panel-header > h1
	margin 10px auto
	font-family $font-content
	font-size $font-size-larger
	font-weight $font-weight-bold

.panel--banner__h3
	font-size $font-size-larger
	font-family $font-header
	font-weight $font-weight-normal-alt
	text-transform uppercase
	white-space nowrap
	overflow hidden
	text-overflow ellipsis

// User links
user
	font-weight $font-weight-bold
	transition color $animation-short
	cursor pointer

// Icon special cases
.icon-ipdb--huge
	font-size 50px
	& + span
		line-height 56px
		margin-left 7px
		vertical-align top

.dl--centered
	margin-bottom 10px
	&:last-child
		margin-bottom 0
	& > dt
		width 49%
	& > dd
		margin-left 51%