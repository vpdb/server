Hi {{ user.name }},

You recently requested the game "{{ gameTitle }}"
to be added to VPDB.

Unfortunately there seems to be something wrong with that request
and we won't be adding the game with that particular IPDB number.

The moderator provided the following reason:

> {{ message }}

We hope that clears it up. If not, feel free to reply to this mail.

Have a nice day,

-The VPDB Server.
