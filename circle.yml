machine:

  timezone:
    Europe/Zurich

  # Version of ruby to use
  node:
    version: 5.3.0

## Customize dependencies
dependencies:
  override:
    - deploy/ci/circle.sh

test:
  override:
    - grunt ci
