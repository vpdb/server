Hi {{{ moderator.name }}},

User {{{ user.name }}} has uploaded a new release called "{{{ release.name }}}" for {{{ game.title }}}. Details can be found here:

    {{{ previewUrl }}}

As a moderator you can discuss the release with the author before approving.

Cheers,

    -The VPDB Server.
