displayName: Users
description: Registered users. **Markdown** *test*.
post:
  description: Registers a new user.
  body:
    application/json:
      example: !include post-req.json
  responses:
    201:
      body:
        application/json:
          example: !include post-res-201.json

get:
  description: List all users.
  securedBy: [ oauth2: { scopes: [ ADMIN, ANON ] } ]