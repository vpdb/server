Hi {{{ moderator.name }}},

User {{{ user.name }}} has uploaded a new backglass for {{{ game.title }}} that needs to be approved. You can do that here:

    {{{ uploadsUrl }}}

Cheers,

    -The VPDB Server.
