// Typography
//
// Below are different text styles used on VPDB.
//
// Styleguide 2

// Body
body
	font-family $font-content
	font-weight $font-weight-normal
	font-size $font-size-normal
	-webkit-font-smoothing antialiased

// Headings
//
// Styleguide 2.1


// Standard Headings
//
// Your standard, everyday headings. The pure headers define font, weight and absolute
// size, while the modifiers take care of color, relative size and special positioning.
// Note that `.h4` headers are usually used by panel headers. Also note that headers are
// per default oneliners, i.e. they don't line wrap but are cut off with an ellipsis.
//
//   * Color - `.h--primary`, `.h--lighter` which changes the color, leaving type and size as-is
//   * Size -  `.h--small`, `.h--smaller` which changes the size, leaving the type and color as-is
//
// .             - Default styles
// .h--primary   - Header in primary color
// .h--lighter   - Header in lighter color
// .h--small     - Small font size (only `h1` and `h2` affected)
// .h--smaller   - Smaller font size (only `h1`, `h2` and `h3` affected)
// .h--no-margin - Without margin, when padding is given by parent.
// .h--code      - With code type face
//
// Markup:
// <h1 class="{$modifiers}">Heading h1</h1>
// <h2 class="{$modifiers}">Heading h2</h2>
// <h3 class="{$modifiers}">Heading h3</h3>
// <h4 class="{$modifiers}">Heading h4</h4>
// <h5 class="{$modifiers}">Heading h5</h5>
// <h6 class="{$modifiers}">Heading h6</h6>
//
// Styleguide 2.1.1

// Page headers, Sidebar header, anything that is upper case and title font
h1
	font-family $font-header
	font-size $font-size-larger-alt
	font-weight $font-weight-normal-alt
	text-transform uppercase
	strong
		font-weight $font-weight-normal

// Release title
h2
	font-family $font-content
	font-weight $font-weight-bold
	font-size $font-size-largest
	text-transform none
	small
		font-size $font-size-largest
		font-weight $font-weight-normal

// Huge modal title
h3
	font-family $font-content
	font-weight $font-weight-thin
	font-size $font-size-huge
	text-transform none
	margin-bottom 15px

// Panel headers
h4, .panel-title
	font-family $font-content
	font-weight $font-weight-bold
	font-size $font-size-normal
	text-transform uppercase

// Small sidebar headers
h5
	font-family $font-content
	font-weight $font-weight-normal
	font-size $font-size-normal
	text-transform uppercase
h6
	font-family $font-content
	font-weight $font-weight-bold
	font-size $font-size-normal
	text-transform none

h1.h--small
	font-size $font-size-normal-alt
	line-height $font-size-normal-alt + 10px
h2.h--small
	font-size $font-size-larger
h1.h--smaller
	font-size $font-size-small-alt
h2.h--smaller
	font-size $font-size-large
h3.h--smaller
	font-size $font-size-small-alt
	font-weight $font-weight-normal
h3.h--large
	font-size $font-size-huge * 1.2
h3.h--larger
	font-size $font-size-huge * 1.5
h4.h--large
	font-size $font-size-large
.h--code
	font-family $font-code

.title
	display flex
	align-items flex-end
	flex-wrap wrap
	> h1
		white-space nowrap
	.title-main
		flex-grow 1
		margin-bottom 0
	.title-sub
		line-height 100%
		margin-top 5px
		margin-bottom 0

// Pre Headings
//
// Sometime you want to prefix a heading with something. Works well with `h4`:
//
// Markup:
// <span class="{$modifiers}">Playfield</span>
// <h4 class="h--large">Screenshot</h4>
//
// .pre-h4            - Header prefix
//
// Styleguide 2.1.2
.pre-h4
	text-transform uppercase
	font-size $font-size-smaller
	font-weight $font-weight-normal
	display block
	margin-bottom -10px

// font weights
b, strong
	font-weight $font-weight-bold

// Make all headings oneliner with ellipsis (except h4, which causes weird behavior in panel heading)
h1, h2, h3, h5, h6
	white-space nowrap
	overflow hidden
	text-overflow ellipsis
	line-height normal
	.svg-icon
		height 30px
		width 30px
		margin-top -2px

h1, h2, h3, h4, h5, h6
	&.h--no-margin
		margin 0
	&.h--shift-up
		margin -10px 0 5px
	&.h--multiline
		line-height 36px
		white-space normal
		overflow inherit

// Fix vertical position of floating h
h1 + h1.pull-right
	margin-top -44px
	margin-bottom 0

// icons within h2
h2.h--smaller > .fa-2x
	font-size 30px

.text--large
	font-size $font-size-large
.text--shift-up
	margin-top -10px
	margin-bottom -10px

// User links
user
	font-weight $font-weight-bold
	transition color $animation-short
	cursor pointer

code, kbd, pre, samp, tt, .code
	font-family $font-code
	font-size $font-size-code
tt
	padding 1px 2px
pre
	font-size $font-size-code