Hi {{{ user.name }}},

Your upload {{{ file.name }}} for {{{ game.title }}} has been checked by moderator {{ moderator.name }}.

Unfortunately, {{ moderator.name }} couldn't get your file to play and therefore had to mark it as broken. Here are more details:

{{{ message }}}

If you think that we're missing something, please let us know in the moderation panel here:

    {{{ url }}}

As soon as you upload a fix we'll re-validate it so everybody will know about the fixed version.

Have a great day,

    -The VPDB Server.
