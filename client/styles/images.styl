// Images
//
// Styleguide 7

// Base class
//
// Images are usually displayed as a background image in a `<div>`, using the
// `bg-img` annotation. This allows the image to be properly placed into the
// container for a given aspect ratio. If the aspect ratio doesn't matter and
// an `<img>` element is used, then use the `bg-src` directive for the source
// URL instead of `src`, which insures proper loading and a fade-in effect.
//
// Markup:
// <div class="{$modifiers} img--ar-square" img-bg="/images/styleguide_square.jpg"></div>
// <img class="{$modifiers} img--ar-square padder-top" img-src="/images/styleguide_square.jpg">
//
// .img          - Default image
//
// Styleguide 7.1
.img
	background-repeat no-repeat
	background-position center center
	background-size cover
	opacity 0
	transition opacity $animation-short
	&.loaded
		opacity 1


// Aspect Ratios
//
// Note that with aspect ratio different than square, the `.img` element will
// be displayed at 100% width.
//
// Markup:
// <div class="row">
//   <div class="col-xs-4"><div class="img {$modifiers}" img-bg="/images/styleguide_16_9.jpg"></div></div>
//   <div class="col-xs-4"><div class="img {$modifiers}" img-bg="/images/styleguide_square.jpg"></div></div>
//   <div class="col-xs-4"><div class="img {$modifiers}" img-bg="/images/styleguide_banner.jpg"></div></div>
// </div>
//
// .img--ar-square          - Square (1:1)
// .img--ar-bg              - Backglass (5:4)
// .img--ar-logo            - Logo
//
// Styleguide 7.2
.img--ar-bg, .img--ar-logo, .img--ar-square
	position relative
	width 100%
	height 0
.img--ar-bg
	padding-bottom 80% // % of width, defines aspect ratio
.img--ar-logo
	padding-bottom 40%
.img--ar-square
	padding 0
	width $list-square-width
	height $list-square-width

	&.img--sm
		width 48px
		height 48px


// Top right table logo
.img--logo
	margin-top 30px
	width 95%



// Avatar images (size and border-radius) - default is --md
.img-avatar
	width 64px
	height 64px
	border-radius 50%
	padding-bottom 0
.img-avatar--lg
	width 72px
	height 72px
.img-avatar--sm
	width 48px
	height 48px

// Avatar place holders
.avatar-placeholder:before
	position absolute
	font-family FontAwesome
	font-style normal
	font-weight normal
	line-height 1
	-webkit-font-smoothing antialiased
	-moz-osx-font-smoothing grayscale
	content '\f007'
	margin-left 19px // --md
	margin-top 14px
	font-size 33px
.avatar-placeholder--sm:before
	margin-left 18px
	margin-top 16px
	font-size 37px
.avatar-placeholder--lg:before
	margin-left 18px
	margin-top 16px
	font-size 37px


// Rounded corners
.img--rounded // formerly: .img-fullspan
	padding 0
	border-radius $border-radius-small
.img--rounded-top
	border-top-radius($border-radius-small)
.img--rounded-left
	border-left-radius($border-radius-small)
