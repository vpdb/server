Hi {{{ user.name }}},

User {{ uploader.name }} just uploaded a new version {{ version.version }} of {{ release.name }} for {{ game.title }}. Changes are:

{{{ changes }}}

For more details, check it out here:

    {{{ url }}}

Have a great day!

    -The VPDB Server.
