Hi {{{ moderator.name }}},

Contributor {{{ user.name }}} has published a new release called "{{{ release.name }}}" for {{{ game.title }}}. Here's the URL:

    {{{ url }}}

Cheers,

    -The VPDB Server.
