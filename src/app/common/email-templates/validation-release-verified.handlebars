Hi {{{ user.name }}},

Good news, your file {{{ file.name }}} for {{{ game.title }}} has been validated.

That means that moderator {{ moderator.name }} downloaded your release, played and examinated it, and was fully happy with it!

Here are the comments:

{{{ message }}}

As always you can privately message the mods through the moderation panel:

    {{{ url }}}

Have a great day,

    -The VPDB Server.
