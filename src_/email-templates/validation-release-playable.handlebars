Hi {{{ user.name }}},

Your upload {{{ file.name }}} for {{{ game.title }}} has been validated.

That means that moderator {{ moderator.name }} downloaded your release, played and examinated it. Good news is that there weren't any major problems.

However, here are a few suggestions on how to make your release even better:

{{{ message }}}

If you have any questions, you can always let us know through the moderation panel of your release:

    {{{ url }}}

Have a great day,

    -The VPDB Server.
