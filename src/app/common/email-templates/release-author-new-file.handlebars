Hi {{{ user.name }}},

User {{ uploader.name }} just uploaded "{{ file.name }}" for version {{ version.version }} of {{ release.name }} for {{ game.title }}.

For more details, check it out here:

    {{{ url }}}

Have a great day!

    -The VPDB Server.
